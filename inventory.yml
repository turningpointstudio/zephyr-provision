# mediaservers:
#   hosts:
#     hive:
#       ansible_host: 10.40.3.234 # can also be a hostname
#   vars: # these variables are set for all hosts in the group
#     ansible_user: ansible # username used to login into the host with
#     ansible_password: ansible # password used to login into the host with
#     ansible_port: 5985
#     ansible_connection: winrm
#     ansible_winrm_transport: basic
#     ansible_winrm_scheme: http
#     ansible_winrm_message_encryption: never

myhosts:
  hosts:
    workstation_1:
      ansible_host: 192.168.4.121
  vars:
    ansible_user: volare
    ansible_password: volare2025!
    ansible_port: 5986
    ansible_connection: winrm
    ansible_winrm_transport: basic
    ansible_winrm_scheme: https
    ansible_winrm_server_cert_validation: ignore

# onprem:
#   children:
#     mediaservers:
#     workstations:
#     userstations:
#     stagemachines:
#     webservers: