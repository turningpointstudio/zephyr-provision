---
# tasks file for base
- name: Create Project Specific User
  win_user:
    name: "{{ project_user }}"
    password: "{{ project_password }}"
    fullname: "{{ project_user }} User"
    description: Project User
    user_cannot_change_password: true
    groups:
      - Administrators