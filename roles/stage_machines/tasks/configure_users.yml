---

- name: Create {{ stage_user }} User
  win_user:
    name: "{{ stage_user }}"
    password: "{{ stage_password }}"
    fullname: "{{ stage_user }}"
    user_cannot_change_password: true
    groups:
      - Administrators

- name: Hide {{ project_user }} User
  win_regedit:
    path: HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon\SpecialAccounts\Userlist
    name: "{{ project_user }}"
    type: dword
    data: 0