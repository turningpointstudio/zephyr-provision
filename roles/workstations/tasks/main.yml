---

- include_tasks: "{{ work_stations_tasks }}"
  loop:
    # - create_project_folders.yml
    # - map_network_drives.yml
    # - install_software.yml
    - configure_deadline_repository.yml
  loop_control:
    loop_var: work_stations_tasks