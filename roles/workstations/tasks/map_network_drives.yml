--- 

- name: Remove any existing M drive mapping
  win_mapped_drive:
    letter: M
    state: absent

- name: Map Drive for user
  block:
    - name: Save the network credentials
      win_credential:
        name: 192.168.30.194
        type: domain_password
        username: ss25
        secret: ss25-password
        state: present

    - name: Map the drive
      win_mapped_drive:
        letter: M
        path: "\\\\192.168.30.194\\Park Delivery"
        state: present

  vars:
    ansible_become: true
    ansible_become_method: runas
    ansible_become_user: '{{ project_user }}'
    ansible_become_pass: '{{ project_password }}'